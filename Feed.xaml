<Window x:Class="CRUD.Feed"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:CRUD"
        xmlns:modelos="clr-namespace:CRUD.Modelos"
        mc:Ignorable="d"
        Title="Feed" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Título do Feed -->
        <TextBlock Text="Feed Social" FontSize="24" FontWeight="Bold"
                   Foreground="Chartreuse" Margin="10,0,10,15" Width="200" />

        <!-- Container do Feed (Lista de Cards) -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Card Exemplo 1 -->
                <ItemsControl x:Name="ItemsControlFeed">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type modelos:Postagem}">
                            <Border Background="White" CornerRadius="10" Margin="0,0,0,15" Padding="15" Width="400">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" Color="#000000" Opacity="0.06" ShadowDepth="2" />
                                </Border.Effect>

                                <StackPanel>
                                    <!-- Cabeçalho: Nome, Username e Data -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Usuario.Nome}" FontWeight="Bold" FontSize="14"
                                                       Foreground="#050505" />
                                            <TextBlock Text="{Binding Usuario.Username, StringFormat='@{0}' }" Margin="6,0,0,0" Foreground="#65676B"
                                                       FontSize="13"
                                                       VerticalAlignment="Center" />
                                        </StackPanel>

                                        <TextBlock Text="{Binding Postado_em, StringFormat='dd MMM - HH:mm'}" Grid.Column="1" Foreground="#65676B"
                                                   FontSize="12"
                                                   VerticalAlignment="Center" />
                                    </Grid>

                                    <!-- Conteúdo do Post -->
                                    <TextBlock
                                        Text="{Binding Conteudo}"
                                        FontSize="14" Foreground="#050505" TextWrapping="Wrap" Margin="0,0,0,15"
                                        LineHeight="20" />

                                    <!-- Botão Curtir (Estado Padrão) -->
                                    <Button Content="❤️ Curtir" HorizontalAlignment="Left" Padding="15,6" Cursor="Hand"
                                            Background="Transparent" BorderThickness="1" BorderBrush="#CED0D4"
                                            Foreground="Chartreuse" FontWeight="SemiBold" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>